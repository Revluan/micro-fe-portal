version: '3.8'
services:
  frontend:
    build:
      context: .
      dockerfile: docker/Dockerfile.frontend
    ports:
      - "80:80"
    restart: always

  config-service:
    build:
      context: .
      dockerfile: docker/Dockerfile.service
    ports:
      - "3000:3000"
    restart: always
